#include "Team.hh"

namespace MultiTSP {

Team::Team(unsigned int p_id, unsigned int p_size, unsigned int p_entry_time)
    : id(p_id), size(p_size), entry_time(p_entry_time) {}

} // namespace MultiTSP
